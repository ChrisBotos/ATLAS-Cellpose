1) Created a configuration file for the cellpose scripts.

2) Created many directories and organized the project.
Made many .gitkeep files and updated the .gitignore.

3) Fixed the tile merge issue.

4) Fixed the image area not being divisible by the tile area bug.
Now the tiles start from the top left, so they may cover smaller areas on the bottom and on the right borders due to cropping.

5) Organized the project repository:
- Created a code directory and code subdirectories.
- Created a nuclei_segmentation directory.
- Created a utils directory.
- Created a tests directory.
- Created a configs directory.
- Created a data directory.
- Created a results directory.
- Created a logs directory.
- Created a debug directory.
- Created a debug_snapshots directory.
- Created a .gitignore file.
- Created a README.md file.
- Created a requirements.txt file.

6) Fixed a lot of bugs and created test files for all scripts in the code directory, for example:
- Parallel running problems.
- Gamma Correction bug.

7) Created signatures and explanations at the top of all scripts.

8) Improved memory allocation by not loading the whole image into the RAM and loading tiles separately.

9) Greatly improved tiling and introduced a new merging rule where if only one mask touches a tile border it is not merged with the other but removed.

10) Created a pipeline able to use previous results and skip steps.

11) Created a full_overlay script that uses gpu acceleration and does not load the whole image into the RAM.

12) Fixed a bottom right contrast box creation bug of clahe.

13) Improved the merging of masks by making a new dir called cellpose_merge in code.
This hosts a lot of scripts helping in properly merging the masks according to the 4 step rule.
Also, the big change is that the script utilize gpu acceleration and batching to merge without memory issues.

14) Implemented parallel processing for cellpose segmentation and merging of masks by running many tiles in parallel.

15) Memory safety and management.

16) Created the environment.yml file and tested it crating a suitable conda environment for python3.10.

17) Implemented the two phase merging strategy (vertical overlaps first then horizontal ones).

